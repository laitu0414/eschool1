diff --git a/Services/AnnualScoreService.cs b/Services/AnnualScoreService.cs
--- a/Services/AnnualScoreService.cs
+++ b/Services/AnnualScoreService.cs
@@
-        var rows = grades.Where(g => g.IdHocSinh == student.IdHocSinh &&
-            (g.IdNamHoc == year.IdNamHoc || (g.IdHocKy.HasValue && termIds.Contains(g.IdHocKy.Value)))).ToList();
-        var allSubjectIds = required.Union(rows.Select(g => g.IdMonHoc)).OrderBy(id => id).ToList();
-        foreach (var subjectId in allSubjectIds)
+        // Chỉ lấy điểm thuộc năm đang tổng kết. Dữ liệu cũ có thể chưa có IdNamHoc
+        // nhưng vẫn hợp lệ nếu IdHocKy thuộc đúng năm.
+        var rows = grades.Where(g => g.IdHocSinh == student.IdHocSinh &&
+            (g.IdNamHoc == year.IdNamHoc ||
+             (!g.IdNamHoc.HasValue && g.IdHocKy.HasValue && termIds.Contains(g.IdHocKy.Value)) ||
+             (g.IdHocKy.HasValue && termIds.Contains(g.IdHocKy.Value)))).ToList();
+
+        // Chỉ tổng kết các môn thực sự được phân công cho lớp.
+        // Điểm thừa của môn cũ/không còn phân công không được làm hỏng cả kết quả năm.
+        foreach (var subjectId in required)
         {
             var subject = new AnnualSubjectScore { SubjectId = subjectId, Name = subjects.FirstOrDefault(m => m.IdMonHoc == subjectId)?.TenMon ?? $"Môn {subjectId}" };
             var data = rows.Where(g => g.IdMonHoc == subjectId).ToList();
             decimal? ReadSemester(int term)
             {
-                var matches = data.Where(g => g.IdHocKy == term).ToList();
+                var matches = data.Where(g => g.IdHocKy == term &&
+                    (!g.IdNamHoc.HasValue || g.IdNamHoc == year.IdNamHoc)).ToList();
                 if (matches.Count != 1) return null;
                 var grade = matches[0];
-                if (grade.IdNamHoc != year.IdNamHoc) return null;
                 // Recompute from components; never finalize stale cached averages.
                 grade.TinhDiemTrungBinh();
                 return grade.DiemTB;
             }
             subject.Semester1 = ReadSemester(terms[0].IdHocKy);
             subject.Semester2 = ReadSemester(terms[1].IdHocKy);
             subject.Annual = CalculateAnnual(subject.Semester1, subject.Semester2);
-            if (data.Any(g => g.IdNamHoc != year.IdNamHoc || !g.IdHocKy.HasValue || !termIds.Contains(g.IdHocKy.Value)))
+            if (data.Any(g =>
+                (g.IdNamHoc.HasValue && g.IdNamHoc != year.IdNamHoc) ||
+                !g.IdHocKy.HasValue ||
+                !termIds.Contains(g.IdHocKy.Value)))
             {
                 subject.Annual = null;
                 subject.Error = "Dữ liệu sai năm học/học kỳ.";
@@
-            Rule = "annual-v1", student.IdHocSinh, student.IdLopHoc, student.TrangThai, student.LopHoc.Khoi,
+            Rule = "annual-v2", student.IdHocSinh, student.IdLopHoc, student.TrangThai, student.LopHoc.Khoi,
             year.IdNamHoc, Required = required,
             Terms = terms.Select(t => new { t.IdHocKy, t.NgayBatDau }),
-            Grades = rows.OrderBy(g => g.IdMonHoc).ThenBy(g => g.IdHocKy).ThenBy(g => g.IdDiem)
+            Grades = rows.Where(g => required.Contains(g.IdMonHoc))
+                .OrderBy(g => g.IdMonHoc).ThenBy(g => g.IdHocKy).ThenBy(g => g.IdDiem)
                 .Select(g => new { g.IdDiem, g.IdMonHoc, g.IdHocKy, g.IdNamHoc, g.Diem15Phut, g.Diem1Tiet, g.DiemGiuaKy, g.DiemCuoiKy })
         }))));
diff --git a/Controllers/LenLopController.cs b/Controllers/LenLopController.cs
--- a/Controllers/LenLopController.cs
+++ b/Controllers/LenLopController.cs
@@
-        public async Task<IActionResult> Index()
+        private async Task<NamHoc?> GetProcessingYearAsync()
+        {
+            var years = await _context.NamHocs.AsNoTracking()
+                .OrderByDescending(y => y.NgayBatDau)
+                .ToListAsync();
+            return years.FirstOrDefault(y => y.TrangThai) ?? years.FirstOrDefault();
+        }
+
+        public async Task<IActionResult> Index()
         {
-            var allHocSinh = await _context.HocSinhs.Include(h => h.LopHoc).ToListAsync();
-            var allLops = await _context.LopHocs.ToListAsync();
-            ViewBag.AcademicYears = await _context.NamHocs.AsNoTracking().OrderByDescending(y => y.NgayBatDau).ToListAsync();
+            var academicYears = await _context.NamHocs.AsNoTracking()
+                .OrderByDescending(y => y.NgayBatDau).ToListAsync();
+            var processingYear = academicYears.FirstOrDefault(y => y.TrangThai) ?? academicYears.FirstOrDefault();
+            var processingYearName = processingYear?.TenNamHoc;
+
+            var allHocSinh = await _context.HocSinhs.Include(h => h.LopHoc)
+                .Where(h => h.LopHoc != null && h.LopHoc.NamHoc == processingYearName)
+                .ToListAsync();
+            var allLops = await _context.LopHocs
+                .Where(l => l.NamHoc == processingYearName)
+                .ToListAsync();
+            ViewBag.AcademicYears = academicYears;
+            ViewBag.ProcessingYear = processingYearName;
             var assessments = await BuildPromotionAssessmentsAsync(allHocSinh);
@@
         public async Task<IActionResult> BulkApproveAllEligible()
         {
             if (IsResultsLocked())
                 return RedirectWithLockedResultsMessage();

+            var processingYear = await GetProcessingYearAsync();
+            if (processingYear == null)
+            {
+                TempData["Error"] = "Chưa có năm học để xét lên lớp.";
+                return RedirectToAction(nameof(Index));
+            }
             var candidates = await _context.HocSinhs.Include(h => h.LopHoc)
-                .Where(h => h.TrangThai && !h.DaDuyet).ToListAsync();
+                .Where(h => h.TrangThai && !h.DaDuyet && h.LopHoc != null &&
+                    h.LopHoc.NamHoc == processingYear.TenNamHoc).ToListAsync();
             var assessments = await BuildPromotionAssessmentsAsync(candidates);
             var students = candidates.Where(h => assessments[h.IdHocSinh].IsEligible).ToList();
@@
         public async Task<IActionResult> LockResults()
         {
             if (IsResultsLocked()) return RedirectWithLockedResultsMessage();
-            var students = await _context.HocSinhs.Include(h => h.LopHoc).Where(h => h.TrangThai).ToListAsync();
+            var processingYear = await GetProcessingYearAsync();
+            if (processingYear == null)
+            {
+                TempData["Error"] = "Chưa có năm học để khóa kết quả.";
+                return RedirectToAction(nameof(Index));
+            }
+            var students = await _context.HocSinhs.Include(h => h.LopHoc)
+                .Where(h => h.TrangThai && h.LopHoc != null &&
+                    h.LopHoc.NamHoc == processingYear.TenNamHoc).ToListAsync();
             var assessments = await BuildPromotionAssessmentsAsync(students);
