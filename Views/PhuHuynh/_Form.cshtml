@model eSchool.ViewModels.PhuHuynhViewModel

@{
    var isAdmin = Context.Session.GetInt32("RoleId") == SystemRoleIds.SystemAdmin;
}

<div class="row teacher-admin-form">
    @if (isAdmin && Model.IdPhuHuynh == 0)
    {
        <div class="col-12 mb-3">
            <label asp-for="IdHocSinh" class="form-label">Học sinh liên kết <span class="text-danger">*</span></label>
            <select asp-for="IdHocSinh" asp-items="Model.HocSinhs" class="form-select" required>
                <option value="">-- Chọn học sinh chưa có phụ huynh --</option>
            </select>
            <div class="form-text">Chỉ hiển thị học sinh đang học và chưa có người giám hộ chính.</div>
            <span asp-validation-for="IdHocSinh" class="text-danger"></span>
        </div>
    }
    <div class="col-md-6 mb-3">
        <label asp-for="HoTen" class="form-label">Họ tên <span class="text-danger">*</span></label>
        <input asp-for="HoTen" class="form-control" required maxlength="100" />
        <span asp-validation-for="HoTen" class="text-danger"></span>
    </div>
    <div class="col-md-6 mb-3">
        <label asp-for="Email" class="form-label">Email</label>
        <input asp-for="Email" type="email" class="form-control" maxlength="100" />
        <span asp-validation-for="Email" class="text-danger"></span>
    </div>
    <div class="col-12">
        <div class="alert alert-info py-2 mb-3">
            <i class="bi bi-info-circle me-1"></i>
            Số điện thoại đăng nhập của phụ huynh được lấy tự động từ học sinh liên kết.
        </div>
    </div>
    <div class="col-md-6 mb-3">
        <label asp-for="NgheNghiep" class="form-label">Nghề nghiệp</label>
        <input asp-for="NgheNghiep" class="form-control" maxlength="100" />
        <span asp-validation-for="NgheNghiep" class="text-danger"></span>
    </div>
    <div class="col-12 mb-3">
        <label asp-for="DiaChi" class="form-label">Địa chỉ</label>
        <textarea asp-for="DiaChi" class="form-control" rows="3" maxlength="255"></textarea>
        <span asp-validation-for="DiaChi" class="text-danger"></span>
    </div>
    <div class="col-md-6 mb-3">
        <label asp-for="TrangThai" class="form-label">Trạng thái</label>
        <select asp-for="TrangThai" class="form-select">
            <option value="true">Hoạt động</option>
            <option value="false">Ngừng hoạt động</option>
        </select>
    </div>
</div>
