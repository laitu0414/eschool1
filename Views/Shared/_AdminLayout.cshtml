@inject eSchool.Models.AppDbContext DbContext
@{
    var title = ViewData["Title"] ?? "eSchool Admin";
    var thongBaoCount = DbContext.ThongBaos.Count();
    var thongBaoMoiNhat = DbContext.ThongBaos
        .OrderByDescending(tb => tb.NgayTao)
        .Take(5)
        .ToList();

    string HienThiThoiGian(DateTime ngayTao)
    {
        var khoangCach = DateTime.Now - ngayTao;

        if (khoangCach.TotalMinutes < 1) return "Vừa xong";
        if (khoangCach.TotalHours < 1) return $"{(int)khoangCach.TotalMinutes} phút trước";
        if (khoangCach.TotalDays < 1) return $"{(int)khoangCach.TotalHours} giờ trước";
        if (khoangCach.TotalDays < 30) return $"{(int)khoangCach.TotalDays} ngày trước";

        return ngayTao.ToString("dd/MM/yyyy HH:mm");
    }
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@title - eSchool</title>
    <link rel="stylesheet" href="~/css/student.css" asp-append-version="true" />
    <script src="~/js/student.js" asp-append-version="true"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet" />
    <link href="~/css/admin.css" rel="stylesheet" asp-append-version="true" />
    <link href="~/css/footer.css" rel="stylesheet" asp-append-version="true" />
    <link href="~/css/management-navigation.css" rel="stylesheet" asp-append-version="true" />
    <link href="~/css/management-theme.css" rel="stylesheet" asp-append-version="true" />
    <link href="~/css/pagination.css" rel="stylesheet" asp-append-version="true" />
    <link href="~/css/table-column-manager.css" rel="stylesheet" asp-append-version="true" />
</head>

<body class="management-workspace">
    <div class="app">
        <aside class="sidebar">
            <div class="brand">
                <div class="brand-logo"><i class="bi bi-mortarboard-fill"></i></div>
                <div>
                    <h3>eSchool</h3>
                    <p>Quản trị nhà trường</p>
                </div>
            </div>
            @await Html.PartialAsync("_ManagementNavigation")
            <div class="sidebar-bottom">
                <form asp-controller="Account" asp-action="Logout" method="post">
                    <button type="submit" class="menu-item logout border-0 w-100 text-start">
                        <i class="bi bi-box-arrow-right"></i><span>Đăng xuất</span>
                    </button>
                </form>
            </div>
        </aside>

        <main class="main">
            <header class="topbar">
                <div class="workspace-heading">
                    <small>Hệ thống quản lý nhà trường</small>
                    <h1>@ViewData["Title"]</h1>
                </div>

                <div class="topbar-right">
                                        <div class="dropdown notification-dropdown">
                        <button class="bell bell-btn" type="button" data-bs-toggle="dropdown" aria-expanded="false" title="Thông báo">
                            <i class="bi bi-bell-fill"></i>
                            @if (thongBaoCount > 0)
                            {
                                <span>@thongBaoCount</span>
                            }
                        </button>
                        <div class="dropdown-menu dropdown-menu-end notification-menu">
                            <div class="notification-title">Thông báo</div>
                            @if (thongBaoMoiNhat.Any())
                            {
                                foreach (var thongBao in thongBaoMoiNhat)
                                {
                                    <div class="notification-item">
                                        <strong>@thongBao.TieuDe</strong>
                                        <small>@HienThiThoiGian(thongBao.NgayTao)</small>
                                    </div>
                                }
                            }
                            else
                            {
                                <div class="notification-empty">Chưa có thông báo</div>
                            }
                        </div>
                    </div>

                    <div class="dropdown">
                        <button class="admin-user admin-user-btn" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <div class="avatar">A</div>
                            <div>
                                <strong>@(Context.Session.GetString("Username") ?? "Admin")</strong>
                                <p>Quản trị hệ thống</p>
                            </div>
                            <i class="bi bi-chevron-down"></i>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end admin-user-menu">
                            <li><a class="dropdown-item" asp-controller="TaiKhoan" asp-action="Index"><i class="bi bi-person-badge me-2"></i>Tài khoản</a></li>
                            <li><a class="dropdown-item" asp-controller="NhatKy" asp-action="Index"><i class="bi bi-clock-history me-2"></i>Nhật ký hoạt động</a></li>
                            <li><hr class="dropdown-divider" /></li>
                            <li>
                                <form asp-controller="Account" asp-action="Logout" method="post">
                                    <button type="submit" class="dropdown-item text-danger"><i class="bi bi-box-arrow-right me-2"></i>Đăng xuất</button>
                                </form>
                            </li>
                        </ul>
                    </div>
                </div>
            </header>

            <section class="page">
                @await Html.PartialAsync("_ManagementBackLink")
                @await Html.PartialAsync("_ManagementPageLinks")
                @RenderBody()
            </section>
            @await Html.PartialAsync("_SiteFooter")
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/required-fields.js" asp-append-version="true"></script>
    <script src="~/js/table-pagination.js" asp-append-version="true"></script>
    <script src="~/js/table-column-manager.js" asp-append-version="true"></script>
    <script src="~/js/xlsx.full.min.js" asp-append-version="true"></script>
    <script src="~/js/excel-preview.js" asp-append-version="true"></script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>





